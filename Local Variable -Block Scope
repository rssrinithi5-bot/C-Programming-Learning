#include <stdio.h>

int main()
{
    int x = 10;

    if(x > 5)
    {
        int y = 20;

        printf("x = %d\n", x);
        printf("y = %d\n", y);
    }

    printf("x = %d\n", x);

    return 0;
}
