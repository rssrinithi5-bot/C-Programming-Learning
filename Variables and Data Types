#include <stdio.h>

int main()
{
    int age = 20;
    float temperature = 32.5;
    char grade = 'A';

    printf("Age = %d\n", age);
    printf("Temperature = %.2f\n", temperature);
    printf("Grade = %c\n", grade);

    return 0;
}

Data Type → Variable Declaration → Value Assignment → Processing → Output
